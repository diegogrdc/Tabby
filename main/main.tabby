Program basicProgram;

Var Int n;

Tabby() {
    Read(n);
    If(n > 0) {
        Write("Positive");
    } Else {
        If(n == 0) {
            Write("Zero");
        } Else {
            Write("Negative");
        }
    }
}